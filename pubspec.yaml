name: moto_vitals
description: A new Flutter project tracking vehicle vitals.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^4.15.8 # Always use latest or suitable versions in actual execution
  firebase_auth: ^4.17.8
  firebase_storage: ^11.6.0
  google_fonts: ^6.2.1
  url_launcher: ^6.2.4
  cached_network_image: ^3.3.1
  firebase_core: ^2.32.0
  provider: ^6.1.5+1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
